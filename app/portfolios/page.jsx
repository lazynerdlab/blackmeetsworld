import Portfolios from "@/components/portfolios/Portfolios";
import React from "react";

export const metadata = {
  title: "Portfolios",
  description: "View our works and collections",
};

const page = () => {
  return <Portfolios />;
};

export default page;
