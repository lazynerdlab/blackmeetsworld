import Details from "@/components/portfolios/Details";
import React from "react";

export const metadata = {
  title: "Portfolio Details",
  description: "Portfolio Details Page",
};

const page = () => {
  return <Details />;
};

export default page;
